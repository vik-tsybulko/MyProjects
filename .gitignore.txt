#игнорятся файлы имя которых заканчивается на а
*.a
#но обрабатываем файл lib.a не смотря на предыдущее правило
!lib.a
#игнорируем файл TODO находящийся в корневом каталоге
/TODO
#игнорируем файлы в директории build
build/
#игнорируем все файлы с рашриением .txt в каталоге doc причем файлы в подкаталогах не игнорируются(doc/notes.txt - ignore, doc/server/arch.txt - don`t ignore
doc/*.txt
#игнорируем все файлы в каталоге doc
doc/**/*.txt